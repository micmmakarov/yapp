.the_content
  -if admin?
    =render :partial => "blocks/add_block", :locals => {:video => video}
=render :partial => "blocks/content", :locals => {:blocks => video.blocks}

.the_content
  -if admin?
    =render :partial => "categories/choose", :locals => {:object => video}
